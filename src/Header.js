require("react");
/** @jsx jsx */
import { css, jsx } from "@emotion/core";

const Header = () => {
  return (
    <div css={css``}>
      <h1>Well the Main View is running!</h1>
    </div>
  );
};

export default Header;
