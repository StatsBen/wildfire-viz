require("react");
/** @jsx jsx */
import { css, jsx } from "@emotion/core";

const Title = () => {
  return (
    <div css={css``}>
      <h1 css={css``}>FireViz</h1>
    </div>
  );
};

export default Title;
